'use strict';
const q=document.querySelector('#questions');
function updateCache(){const n=Number(q.value);document.querySelector('#question-value').value=n;document.querySelector('#uncached').textContent=(n*10000).toLocaleString('en-US');document.querySelector('#cached-bar').style.width=(100/n)+'%';document.querySelector('#cache-explanation').textContent=`${n} × 10,000 versus 1 × 10,000 state tokens. ${n===1?'One question gives no repeated-state saving.':'The shared state is reused across '+n+' questions.'}`;}q.addEventListener('input',updateCache);updateCache();
const dotgrid=document.querySelector('#dot-grid');for(let i=0;i<100;i++)dotgrid.append(document.createElement('i'));
const correct=document.querySelector('#correct');function updateCalibration(){const n=Number(correct.value);document.querySelector('#correct-value').value=n;[...dotgrid.children].forEach((dot,i)=>dot.classList.toggle('wrong',i>=n));const suffix=n===80?'This group matches the stated probability.':n<80?'The model is overconfident in this group.':'The model is underconfident in this group.';document.querySelector('#calibration-verdict').textContent=`80 predicted out of 100. ${n} actually correct. ${suffix}`;}correct.addEventListener('input',updateCalibration);updateCalibration();
const benchmarks={mmlu:{title:'Accuracy on 1,000 questions',j:82.9,o:58.8,jlabel:'82.9%',olabel:'58.8%',max:100,direction:'Higher is better',note:'24.1 percentage-point difference. Zero-shot API evaluation on a reused 1,000-question subset; not full-test leaderboard scores.'},boolq:{title:'Accuracy on 3,270 questions',j:91.56,o:89.45,jlabel:'91.56%',olabel:'89.45%',max:100,direction:'Higher is better',note:'2.11 percentage-point difference. Passage-grounded yes/no comprehension; no post-hoc calibration was fitted.'},ece:{title:'Selected-answer calibration error',j:2.51,o:4.05,jlabel:'2.51%',olabel:'4.05%',max:5,direction:'Lower is better',note:'BoolQ, ten equal-width bins. Axis changes to 0–5%. ECE depends on binning and sample size; this is not an accuracy score.'}};
const selector=document.querySelector('#benchmark');function updateBenchmark(){const b=benchmarks[selector.value];document.querySelector('#benchmark-title').textContent=b.title;document.querySelector('#jev-value').textContent=b.jlabel;document.querySelector('#openjev-value').textContent=b.olabel;document.querySelector('#jev-bar').style.width=(b.j/b.max*100)+'%';document.querySelector('#openjev-bar').style.width=(b.o/b.max*100)+'%';document.querySelector('#metric-direction').textContent=b.direction;document.querySelector('#axis-max').textContent=b.max+'%';document.querySelector('#benchmark-note').textContent=b.note;}selector.addEventListener('change',updateBenchmark);
document.querySelector('#share').addEventListener('click',async()=>{const url=location.origin+location.pathname;const status=document.querySelector('#share-status');try{await navigator.clipboard.writeText(url);status.textContent='Link copied. Ready to share.';setTimeout(()=>status.textContent='',4000);}catch{status.textContent='Copy this address: '+url;}});
const navLinks=[...document.querySelectorAll('.contents nav a')];const sections=[...document.querySelectorAll('article>section')];function onScroll(){const distance=document.documentElement.scrollHeight-innerHeight;document.querySelector('#progress').style.width=(distance>0?Math.max(0,Math.min(100,scrollY/distance*100)):0)+'%';let current=sections[0].id;for(const section of sections)if(section.getBoundingClientRect().top<180)current=section.id;navLinks.forEach(a=>{const active=a.hash==='#'+current;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current');});}addEventListener('scroll',onScroll,{passive:true});onScroll();
if(location.hash.startsWith('#s'))requestAnimationFrame(()=>document.getElementById(location.hash.slice(1))?.scrollIntoView());
