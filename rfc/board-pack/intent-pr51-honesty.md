# Intent — board-pack honesty after PR 51 live graph-query chain

PR 51 turned a sentence the board pack repeats into a false one. Several skim surfaces say the graph-query wiring was
built and tested offline but that **no live chain has used it**, that **graph queries are still untested in any live
chain**, and that **the live graph-query gate remains closed**. On 8 September 2026 a live chain ran retrieval through
graph queries on an Enterprise capacity window and completed: the receipt check verified, the consumer released, and
the window closed with every job it owned accounted for.

Correct those sentences and nothing else. The narrow new fact is *one* chain, on a starting point chosen by hand,
under one operator on both legs, with only the approved case run. It is not catalog discovery, not a second identity,
not real cohort data, and not a benchmark — so the "what we still need to prove" claim (all of it at once, on real
cohort data) is unchanged and must survive this pass intact.

The same sentences carry a second stale clause: they say the five legacy reconciliation windows remain unreconciled.
They were reconciled before this run. Fix that where it appears in a sentence being rewritten anyway; do not go
hunting for more.

Keep the human tone. No scoreboard labels, verdict words, pull-request numbers or commit hashes in reader-facing
prose; those belong here and in the spec.
