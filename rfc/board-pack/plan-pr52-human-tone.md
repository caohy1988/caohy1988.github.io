# Plan — whole-pack human tone

Ordered by how badly each block reads today. Word counts are the current visible text.

## `rfc/board-pack/index.html`

1. **Opening assessment (260 words, one block).** Split into: where this is strong; what works today; what we still
   need to prove. The three recorded experiments and the three chains keep their limits; the 8 September chain keeps
   all three of its own.
2. **Diagram plot note (101 words, one sentence chain).** Break into short sentences. Both chain families keep their
   qualifications.
3. **Design intro (189 words).** Lead with what this is, then the experiments, then their limits.
4. **Retrieval paragraph (227 words, "Two limits." mid-paragraph).** Split the walk's result from its limits. Keep
   the Preview option we did not test, the hand-pinned starting point, and the corrected scoping of which chains
   resolved a pin from a catalog read.
5. **Access paragraphs (160 + 220 words).** Keep all five checks and the control, but as a readable list rather than
   a run-on. Keep every open item: the five checks not run inside graph queries, the window since opened carrying one
   identity, hidden identifiers under the operator's identity, mixed publications, and the receipt check's narrower
   caller-binding scope.
6. **Chain section (260 + 237 + 126 + 192 words).** This is where the inventory lives. Move the entry-group
   mechanics, the 23/170 job counts and the dataset bookkeeping into the linked evidence; keep what each run did,
   what it refused, and what it does not show. The 8 September chain keeps its three limits and its four earlier
   attempts.
7. **Where the evidence stands (151 + 310 words).** The 310-word connected-path paragraph is the worst block on the
   page. Split into: the bar; where the 7 September chains reach; where the restricted chain reaches; where the
   8 September chain reaches; what is still missing; the checkpoint. The bar itself is unchanged.
8. **Envelope notes.** Light touch only — they are already one idea each. Trim the longest (the fact-data note) if it
   can be done without losing the snapshot/cut-off point.

## `rfc/board-pack/STORY.md`

9. Evidence bullets carry the same inventory problem. Same treatment: what ran, what it does not show, detail behind
   the link. The connected-path bullet gets the same split as item 7.

## `rfc/index.html`

10. Only the sentences that mirror the pack: status note, "what the examples show", the connected-path bar and the
    current-evidence box. Shorten; keep every limit. The architecture figure caption, crosswalk table and technical
    contract text are not part of this pass.

## Verification

A claim checklist run against the rendered text before and after: the three chains and their limits, the five access
checks and their gaps, G8 at 0 of 9, the partial samples, the proposed-not-accepted thresholds, the combined bar and
the checkpoint date. Plus HTML parse, unique ids and resolvable fragments.

## Out of scope

No spike file. No new evidence. No metric. No merge.
