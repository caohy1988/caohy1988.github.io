# Plan — leadership story

## `rfc/board-pack/index.html`

1. **Opening assessment.** Becomes the arc's second beat: where this is strong, what we know works, what is still
   open, what the pilot is for. The three dated paragraphs collapse into capability statements. The connected run
   keeps its three limits (S7) in the same breath.
2. **The three comparison notes.** Drop the dated references; keep the substance — replayable context does not
   promise identical model answers; access metadata is not enforcement, and the restricted-identity denials were on
   the SQL path only; a job id alone is not enough, and the receipt withholds on mismatch.
3. **Diagram descriptions (wide and narrow) and the plot note.** Same treatment. These are independently readable,
   so the connected run's limits stay local to them.
4. **The envelope section.** The owner sentence loses the name and handle and becomes role language. Each threshold
   note keeps its number and its "proposed, nobody has accepted this" framing, with the lab dates removed. The
   decision becomes the upcoming checkpoint.
5. **Design section.** Intro, retrieval, access and the chain section: rewrite as what the mechanism does and what
   we have and have not shown. This is where the job counts, entry-group mechanics, per-case blow-by-blow and
   teardown bookkeeping come out. Keep one evidence link per claim that needs backing.
6. **Where the evidence stands / the bar.** Becomes "what is still open": the bar, the three partial results stated
   as capabilities, what is missing, the checkpoint.
7. **Punchline and ask.** Unchanged in meaning; tightened if it helps the arc land.

## `rfc/board-pack/STORY.md`

8. Slim to match the page: the scene, the mechanism, what is known, what is open, the ask. The dated evidence-bullet
   list and its inventories go; a short capability summary replaces them. This file feeds the page, so the bans
   apply to it.

## `rfc/index.html`

9. Only the board-pack mirror surfaces: status note, "what the examples show", the connected-path bar, the
   current-evidence box. Same voice, same bans. The RFC's own dated provenance line is document history, not board
   copy, and stays — as do the architecture figure, crosswalk table and technical contracts.

## Verification

`/tmp/okf-bp-story/claims.py`: capability claims present, three bans clean on the board pack and STORY, HTML parses,
ids unique, fragments resolve. Report the RFC's own changelog dates as out of scope rather than silently passing.

## Out of scope

No spike file. No new evidence or metric. No merge.
