# Spec — BQAA → derived OKF prototype

Slice of `intent.md`. Target: static GitHub Pages demo plus fixture data copied from Phase 0. No OKF core change.

## Beats (must be clickable)

| # | Name | Visitor sees | Must prove |
|---|---|---|---|
| 1 | Observe | A short BQAA event list (TOOL_STARTING / TOOL_COMPLETED / TOOL_ERROR or native `agent_events` shape) about the Germany active-customer-revenue question | Events are observer-only. No query text, principal, source path, or `concept_version_id`. Optional `context_ref` attribute only. |
| 2 | Adapt | One-way adapter output: a derived OKF v0.2 bundle (markdown/JSON tree) | Banner `derived/demo`. Distinct `bundle_key` from authored `cymbal-finance-core`. Does not rewrite `okf-phase0-mvp/fixture/bundle`. |
| 3 | Project | Two panes: Knowledge Catalog discovery (entries + `okf` aspect pins) and BigQuery serving (normalized runtime rows) | Same `publication_id` on both. Catalog pane labeled projection/demo (no live `kcmd` write). Serving pane is client-side rows shaped like the RFC BigQuery projection. |
| 4 | Consume | ADK agent retrieve + (optional) attested-computation stub. **Model: `gemini-3.8-flash`** (Gemini 3.8 Flash, GA 2026-09-02). | Tool result JSON contains `context_ref` and nothing from the never-emit list. Receipt is `UNVERIFIABLE` with reason `phase0_no_execution_or_integrity_proof`, matching golden honesty. A Phase-4 `ATTESTED` shape may sit beside it, labeled non-normative. The agent is ADK (`google.adk.agents.Agent` / `Gemini(model="gemini-3.8-flash")`). Do not default to `gemini-2.5-flash`, `gemini-3.5-flash`, or `gemini-flash-latest`. |

Stepper UI: Next / Back, deep-linkable (`#beat=1` … `#beat=4`). Highlight the federated/observer seam the way glance catalog highlighted the federated beat.

## Identity

Reuse Phase 0 golden identities for the **authored** Cymbal fixture (display-only, "canonical authored knowledge"):

- `observation_id` `sha256:89de188ee22f0b1f06eff6d29c97c841f5757a3a9ca5d879bbd28b2f07ce3b51`
- `snapshot_id` `sha256:e2c0d16a9039d9fac25143bdbe8225c36140cccfc849642ee2d47a173ffa72c7`
- `publication_id` `sha256:e92827f8b351a4dbac14ae3074ef84887c611a303878c116c0c376eacb10cb41`

The **derived** bundle gets its own `bundle_key` (e.g. `bqaa-derived-cymbal-demo`) and its own observation/snapshot/publication triple. If the in-browser adapter cannot reproduce PROFILE.md hashing, pin a second golden file generated by `okf-phase0-mvp/golden/vectors_gen.py` (or a small JS port of the domain-separated SHA-256 rules for the derived closure). Do not invent hashes. Do not reuse the authored triple for derived output.

## Never-emit (telemetry / tool results)

`concept_version_id`, bundle-relative paths, principal, query text, named parameter values, destination table ids as raw names. Agent-facing payloads carry `context_ref` (opaque) plus RFC-allowed receipts. UI may show authored paths in the **bundle inspector** (that's source, not telemetry).

## Data

- Copy (do not import as a new stack) the Phase 0 fixture tree into `rfc/demo/fixture/` as static assets.
- Add `rfc/demo/traces/bqaa-germany.json`: a small observer trace (≤20 events) that a BQAA writer could have emitted while an agent asked the Germany question. Synthetic is fine if labeled. Must be valid-looking BQAA events.
- Adapter is JavaScript in the page (or a tiny precomputed derived bundle JSON the page applies). No build step required for Pages. Vanilla JS matching `evalbench/app.js` is preferred over a bundler.

## Web UI

This is a product demo, not a JSON dump. Bar is `evalbench/index.html` (six-act deck, masthead, stepper, named beats).

- Full-page layout: masthead, four-beat stepper with keyboard next/back, identity chips (`observation_id` / `snapshot_id` / `publication_id`), derived/demo banner.
- Reuse RFC tokens (`--telemetry`, `--catalog`, `--runtime`, Space Grotesk / Source Serif / IBM Plex Mono). Catalog pane uses `--catalog`, BQ pane `--runtime`, observer events `--telemetry`.
- Project beat is a real two-column UI (Catalog cards + BQ table), not two `<pre>` blocks.
- Consume beat shows ADK chrome with **`gemini-3.8-flash`** as a visible model badge, then tool JSON + `context_ref` + UNVERIFIABLE receipt.
- Deep-linkable `#beat=1`…`#beat=4`. No bundler.

## E2E recording (follow-up, not this PR)

Haiyuan, 2026-09-02 3:03 PM PT: **do not record until Claude, Codex, and Kimi have aligned** on the demo UI (dual LGTM on the GitHub PR). This slice ships the clickable page only. After dual LGTM, a follow-up records `rfc/demo/okf-bqaa-e2e.mp4` (~60–120s) and embeds it, evalbench-style. Do not block the UI PR on ffmpeg, Playwright, or screencapture.

## RFC page wiring

- Keep `rfc/index.html` as the RFC. Add a visible "Prototype demo" callout near the Phase 0 MVP note (around the existing `okf-phase0-mvp/` note) linking to `rfc/demo/`.
- Demo lives at `rfc/demo/index.html` so the live URL is `https://caohy1988.github.io/rfc/demo/`.
- Visual language: reuse RFC CSS variables (`--telemetry`, `--catalog`, `--runtime`, Space Grotesk / Source Serif / IBM Plex Mono).

## Tests / gates

- Static: demo loads with no console errors on the four beats (manual; Claude records a short checklist in `rfc/demo/README.md`).
- Fixture: derived bundle still has zero new required OKF keys (frontmatter `type` present on non-reserved `.md`; `log.md` reserved).
- Never-emit scan: a tiny test or page assertion that beat-4 tool JSON keys ∩ never-emit list = ∅.
- No live GCP calls from the page. No secrets.

## Non-goals in this PR

Live `kcmd` upsert, live BigQuery DML, attester service, changing PROFILE.md hashing, merging SDK PRs, starting the six-week clock.


## ADK demo model

Haiyuan, 2026-09-02: the consuming agent in this prototype is an ADK agent whose model id is **`gemini-3.8-flash`**. Show that id in the Consume beat chrome. Browser demo may replay a fixture transcript produced by that agent; if a live ADK process is included, it must construct `Agent(model=Gemini(model="gemini-3.8-flash"), ...)` (or `model="gemini-3.8-flash"`). Env override `DEMO_MODEL_ID` may exist but the checked-in default is `gemini-3.8-flash`.
