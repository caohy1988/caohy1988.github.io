# Intent — `/rfc/detailed-rfc/` as the engineering diligence companion to the decision brief

**Ask (Haiyuan, 2026-09-10 ~00:18 PT, GO on slice `rfc-detailed-align`).** After PR 66 made `/rfc/` a five-minute leadership decision brief, bring the full RFC at `/rfc/detailed-rfc/` into agreement with it: same decision, same honesty, same role names for Knowledge Publications (proposed) and BigQuery Agent Analytics, same three-row evidence status. The detailed page stays the long form for engineers; it does not become a second skim or a third page.

**Why.** A leader who reads the brief and then opens the detailed page should meet the same decision, the same asks, the same checkpoint and the same evidence status in the first screen, then the architecture, protocol, phases and evidence artifacts underneath. Today the detailed page opens with "runtime context projection", names the feature only as "the proposed BigQuery runtime", reaches the pilot ask and the 2026-09-19 checkpoint only in its closing section, and states the concurrency shortfall and the unmeasured full-path cost only inside the recorded-examples note near the end.

**What changes.** A decision block in the masthead (decision requested, status, checkpoint 2026-09-19 as continue / narrow / stop, link to the 5-minute decision brief); the four service roles renamed to the brief's language (BigQuery Knowledge Publications, proposed; BigQuery Agent Analytics, observes use and does not grant access or certify results); a one-line naming bridge explaining that the page's "BigQuery runtime" is the brief's Knowledge Publications, SQL first, receipt as the follow-on; a three-row evidence status (Recorded feasibility / Measured retrieval / Still to validate, each with a scope sentence) in the summary, with the concurrency shortfall and the unmeasured full-path cost visible; the closing gains the product ask and the four requested roles with no names.

**What does not change.** Architecture, six agreements, state machine, design folds, reproducibility, acceptance, phases, the twelve-row risk table, the recorded-examples note, the Prototype callouts, both diagrams, every id, every tested href, the pinned evidence links and the footer. `/rfc/index.html` is not edited. Word ceiling 7,500 folds-open holds.

**Non-goals.** No `/rfc/exec/` route; no receipt-first reopen; no live cloud run; no rendered-word ceiling for the detailed page; no renaming of "BigQuery runtime" throughout the technical sections (the bridge sentence does that job once).
